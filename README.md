# Remote Control

A small remote control web application. Currently you may control your system volume on MacOS.

Run the server and navigate to `http://localhost:8001`. Probably don't expose this to the internet.
